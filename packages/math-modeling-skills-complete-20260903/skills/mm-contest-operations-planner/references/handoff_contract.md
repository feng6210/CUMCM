# 分问交接最小字段

```yaml
question_id:
model_version:
assumptions:
code_entry:
result_files:
main_numbers:
figures:
validation_status:
latex_section:
unresolved_items:
```

交接文件不包含未运行的“预期结果”。若结果未验证，`validation_status` 必须如实记录 `NOT_EVALUATED`、`PARTIAL` 或 `FAIL`。
