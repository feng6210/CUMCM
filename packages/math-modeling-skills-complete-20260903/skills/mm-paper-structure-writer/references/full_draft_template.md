# 完整 LaTeX 初稿模式

完整初稿默认使用 `assets/cumcm-2026` 的中文 CUMCM LaTeX 模板。输出是供用户修改和编译的 `paper/` 目录，不是已经提交的终稿。

只填入已提供或已运行验证的模型、结果、图表和文献。每项关键数值必须能回指机器可读结果；无法验证的内容保留待补位置，而不是虚构。

允许的占位只有：

- `[待填写：题目或关键词]`
- `[待核验并补充：正式参考文献]`
- `[待确认：用户提供的官方模板字段]`

不得为模型方程、关键结果、验证结论、失败状态、作者信息或学校信息创建掩盖性占位符。
