interface:
  display_name: "数学建模：摘要优化"
  short_description: "按分问事实链压缩模型与结果，仅保留改变答案含义的验证和边界。"
  brand_color: "#2563EB"
  default_prompt: "请使用 $mm-abstract-polisher 按每问的输出目标、本题化模型、求解和关键结果检查并优化中文数学建模摘要；验证只保留最有区分度的一项，限制仅在会改变答案含义时进入摘要，避免审计式和模板化表达。"
policy:
  allow_implicit_invocation: true
